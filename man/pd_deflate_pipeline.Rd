% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_deflate_pipeline.R
\name{pd_deflate_pipeline}
\alias{pd_deflate_pipeline}
\title{Batch-deflate every survey in the PIP master inventory}
\usage{
pd_deflate_pipeline(
  inv = NULL,
  force = FALSE,
  verbose = getOption("pipdata.verbose", default = TRUE),
  bootstrap = FALSE,
  bootstrap_entities = NULL,
  dependency_plan = NULL
)
}
\arguments{
\item{inv}{A master inventory \code{data.table} (as returned by
\code{\link[pipload:load_pip_master_inventory]{pipload::load_pip_master_inventory()}}). Default \code{NULL}, in which case the
master inventory is loaded internally.}

\item{force}{Logical. If \code{TRUE}, re-deflate every row regardless of the
\code{deflated} column. Default \code{FALSE}.}

\item{verbose}{Logical. Controls verbosity of downstream
\code{\link[pipload:load_pip_master_inventory]{pipload::load_pip_master_inventory()}} and \code{\link[pipload:pip_write]{pipload::pip_write()}} calls.
Default: \code{getOption("pipdata.verbose", default = TRUE)}.}

\item{bootstrap}{Logical. Explicitly permit unknown-provenance work.}

\item{bootstrap_entities}{Optional restrictive bootstrap identifiers.}

\item{dependency_plan}{Optional advisory plan, revalidated before writes.}
}
\value{
The updated master inventory \code{data.table} (rows deflated in this run
have \code{deflated = TRUE}, \code{content_hash_deflated} and
\verb{aux_*_hash_at_deflation} populated). Side effects: when at least one
survey is processed, writes deflated artifacts to the \code{"pip_deflated"}
alias, writes the updated master to \code{"pip_master"}, and logs a
\code{deflate_summary_inf} entry.
}
\description{
Iterates over the master inventory of cleaned surveys, deflates each via
\code{\link[=pd_deflation]{pd_deflation()}} (simple Mode B: \code{pip_id} only, no \code{version} hint), correctly
detects and skips failures -- including \code{NA} returns from \code{\link[=pd_deflation]{pd_deflation()}}
-- saves each successful deflated survey to the dedicated \code{"pip_deflated"}
stamp alias, updates the master inventory with deflation columns, and logs a
structured \code{deflate_summary_inf} summary entry to \code{"pipdata_log"}.
}
\details{
This function is an independent pipeline stage: it is \strong{not} called by
\code{\link[=pd_process_data]{pd_process_data()}}. Run it after cleaning (and after the \code{"pip_deflated"}
alias is registered, e.g. via \code{\link[=pipdata_dlw_process]{pipdata_dlw_process()}} or an explicit
\code{stamp::st_init()}).

First version policy (R7): everything not yet deflated is deflated. There is
no incremental aux-hash gating yet -- pass \code{force = TRUE} to re-deflate
surveys whose \code{deflated} column is already \code{TRUE}.

\strong{Logging}: writes a \code{deflate_summary_inf} info entry to \code{"pipdata_log"}
with pinned keys \code{n_total}, \code{n_success}, \code{n_failed}, \code{surveys_success}, and
\code{surveys_failed} whenever at least one survey was processed. Per-survey
failures are logged as \code{error} entries (\code{deflation_na},
\code{deflate_save_error}, or the underlying condition class) with the survey
id. A missing \code{content_hash_deflated} for a deflated survey is logged
(\code{deflate_provenance_missing}), never silent.

\strong{Column provenance}:
\itemize{
\item \code{deflated} -- logical; \code{TRUE} for surveys successfully deflated.
\item \code{content_hash_deflated} -- \code{content_hash} of the \code{"pip_deflated"}
artifact (queried from stamp after the run).
\item \code{aux_cpi_hash_at_deflation}, \code{aux_ppp_hash_at_deflation},
\code{aux_pop_hash_at_deflation} -- aux \code{content_hash} resolved once per run by
\code{\link[=get_aux_hashes]{get_aux_hashes()}}, snapshot on the deflated rows. These describe the aux
catalog state when the pipeline ran; the exact aux vintage consumed by
\code{\link[=pd_deflation]{pd_deflation()}} is the one embedded in the survey's \code{pip_meta} artifact
(pinned by \code{version_id_metadata}), not this snapshot.
}
}
\examples{
\dontrun{
release <- "20260401"
pipfun::setup_working_release(release, "TEST", verbose = FALSE)
stamp::st_init(
  root = fs::path(getOption("pipfun.main_dir"), "pip_repository", "pip_deflated"),
  alias = "pip_deflated"
)
new_pip_inv <- pd_deflate_pipeline(force = TRUE, verbose = TRUE)
}
}
\seealso{
\code{\link[=pd_process_data]{pd_process_data()}} for the cleaning stage, \code{\link[=pd_deflation]{pd_deflation()}} for
single-survey deflation, \code{\link[=log_report]{log_report()}} for the report that renders the
\code{deflate_summary_inf} entry.
}
\concept{pd_deflate_pipeline pipeline}
