% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dlw_store_release.R
\name{dlw_store_release}
\alias{dlw_store_release}
\title{Store a DLW release record in \verb{_release/} folder, updates master inventory list.}
\usage{
dlw_store_release(
  pip_raw_inventory_df,
  release_label,
  release_folder,
  update_inventory_list = TRUE,
  pip_raw_releases = NULL,
  log_err = TRUE,
  skip_err = TRUE
)
}
\arguments{
\item{pip_raw_inventory_df}{data.frame. A data frame of pipeline rows (survey_id, file paths, etc.)}

\item{release_label}{Character. e.g. "20250202_INT"}

\item{release_folder}{Character. Path to the folder in which a \verb{_release/}
subfolder is created if it does not exist.}

\item{update_inventory_list}{Logical. If \code{TRUE}, also load/save a \code{.qs} file
storing a named list of releases at \code{pip_raw_releases}.}

\item{pip_raw_releases}{Character. Path to a \code{.qs} file that holds a named list
of releases. Only used if \code{update_inventory_list=TRUE}.}

\item{log_err}{Logical. Whether to log errors via \code{add_log()}.}

\item{skip_err}{Logical. Whether to \emph{skip} errors (instead of aborting).}
}
\value{
Invisibly returns the updated master list (if \code{update_inventory_list=TRUE}),
otherwise returns \code{NULL}. If an error occurs and \code{skip_err=TRUE}, returns \code{NULL}.
}
\description{
By default, this function writes the contents of \code{pip_raw_inventory_df} into
a new \code{.qs} file in a \verb{_release/} folder (one file per release).
If \code{update_inventory_list = TRUE}, it also loads or creates a master list
(pointed to by \code{pip_raw_releases}), appends the release, and saves it back.
}
\examples{
\dontrun{
# Default usage (no skip, log errors, update master list):
dlw_store_release(
  pip_raw_inventory_df  = pip_raw_inventory_df,
  release_label         = "20250202_INT",
  release_folder        = "pip_raw",
  update_inventory_list = TRUE,
  pip_raw_releases      = "pip_raw/pip_raw_inventory_releases.qs",
  log_err  = TRUE,
  skip_err = TRUE
)
}
}
