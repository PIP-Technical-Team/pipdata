% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipdata_dlw_validation.R
\name{dlw_validation}
\alias{dlw_validation}
\alias{dlw_validation_gpwg}
\alias{dlw_validation_group}
\alias{dlw_validation_bin}
\alias{dlw_validation_hist}
\alias{dlw_validation_all}
\alias{dlw_validation_aspire}
\alias{dlw_validation_l}
\alias{dlw_validation_skip}
\title{Validate DLW data (Generic Documentation)}
\usage{
dlw_validation(dlw_data, svy_id)

dlw_validation_gpwg(dlw_data, svy_id)

dlw_validation_group(dlw_data, svy_id)

dlw_validation_bin(dlw_data, svy_id)

dlw_validation_hist(dlw_data, svy_id)

dlw_validation_all(dlw_data, svy_id)

dlw_validation_aspire(dlw_data, svy_id)

dlw_validation_l(dlw_data, svy_id)

dlw_validation_skip(dlw_data, svy_id)
}
\arguments{
\item{dlw_data}{A DLW dataset in \code{qs} format.}

\item{svy_id}{A survey identifier extracted from the dataset.}
}
\value{
A data.frame containing validation results.

An empty data.frame with minimal checks applied.
}
\description{
This is a generic validation interface for DLW datasets across different module types.
Specific functions handle validation logic for GPWG, GROUP, BIN, HIST, ALL, ASPIRE, and L module types.
}
\details{
Deprecated: use \code{dlw_validation_engine()} with \code{module = "gpwg"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "group"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "bin"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "hist"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "all"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "aspire"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "?"} instead.

Deprecated: use \code{dlw_validation_engine()} with \code{module = "skip"} instead.
}
\section{Functions}{
\itemize{
\item \code{dlw_validation_gpwg()}: Validate GPWG data

Performs variable and structural checks on GPWG data, such as availability of core variables,
non-missingness, valid value ranges, and duplication checks.

\item \code{dlw_validation_group()}: Validate GROUP data

Checks for missing values, type mismatches, and invalid entries in GROUP datasets.

\item \code{dlw_validation_bin()}: Validate BIN data

Performs structural and value-based validation for BIN datasets,
checking numeric, character, and key variable consistency.

\item \code{dlw_validation_hist()}: Validate HIST data

Conducts data validation for HIST datasets, including checks for key variables like
\code{urban}, \code{weight}, and \code{welfare}, as well as common structural validations.

\item \code{dlw_validation_all()}: Validate ALL data

Validates general ALL module type data containing core variables such as \code{welfare}, \code{weight}, and optionally \code{urban}.
Ensures basic structure and NA thresholds.

\item \code{dlw_validation_aspire()}: Validate ASPIRE data

Handles validation for ASPIRE DLW datasets by checking structure and numeric variable consistency.
Special attention is paid to \code{hhweight}, \code{urban}, and household size.

\item \code{dlw_validation_l()}: Validate Labor (L) DLW data

Validates DLW datasets containing labor-specific data, such as employment status (\code{lstatus}, \code{empstat}),
person-level identifiers (\code{hhid}, \code{pid}), and working hours (\code{whours}).

\item \code{dlw_validation_skip()}: Skip Validation

Used for DLW modules that require no validation. Ensures only that the dataset is not blank.

}}
\examples{
\dontrun{
dlw_validation_gpwg(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_group(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_bin(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_hist(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_all(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_aspire(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_l(
  dlw_data = "data/dlw_qs",
  svy_id = "survey_id",
)
}
\dontrun{
dlw_validation_skip(
  dlw_data = "data/dlw_qs",
  svy_id = survey_id
)
}
}
\keyword{internal}
