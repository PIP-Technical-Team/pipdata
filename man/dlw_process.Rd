% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dlw_process.R
\name{dlw_process}
\alias{dlw_process}
\title{Process DLW data}
\usage{
dlw_process(
  root_dir = Sys.getenv("PIP_ROOT_DIR"),
  maindir = "PIP_ingestion_pipeline_v2/dlw_repository",
  dlw_inv_folder = "dlw_inventory",
  dlw_pin_folder = "dlw_data",
  inv_gmd_list = "dlw_gmd_inv",
  inv_qs_list = "dlw_pin_inv",
  validation_report_path = "validation_report",
  dlw_inv_list = FALSE,
  dlw_qs_pinned = TRUE,
  log = TRUE,
  save_log = TRUE
)
}
\arguments{
\item{root_dir}{Character. Root directory (pip).  Defaults to the environment variable "PIP_ROOT_DIR".}

\item{maindir}{Character. Main directory where the DLW datasets are saved.}

\item{dlw_inv_folder}{Character. Name of the folder containing the inventory files. Default is "dlw_inventory".}

\item{dlw_pin_folder}{Character. Pin board folder containing .qs files.}

\item{inv_gmd_list}{Character. Name of inventory file that contains GMD datasets list}

\item{inv_qs_list}{Character. Path where inventory file is resided}

\item{validation_report_path}{Character. Path to validation report}

\item{dlw_inv_list}{Logical. If \code{TRUE}, generate GMD inventory list \code{.qs} format.}

\item{dlw_qs_pinned}{Logical. If \code{TRUE}, pinned (\code{.qs} format) and validate modular GMD datasets.}

\item{log}{TRUE/FALSE default value is \code{TRUE}}
}
\value{
Invisibly returns \code{NULL}. Output files are written to disk.
}
\description{
A wrapper function that performs two main tasks:
\enumerate{
\item Generate GMD catalog inventory file in \code{.qs} format via \code{dlw} package.
\item Get datasets from GMD catalog (pinned them in a specific folder), validate and and generate inventory of the datasets as \code{.qs} format
}
}
\examples{
\dontrun{
dlw_process(root_dir = "/path/to/root",
            maindir = "dlw_repository",
            dlw_inv_folder = "dlw_inventory",
            dlw_pin_folder = "dlw_pin",
            inv_gmd_list = "dlw_gmd_inv",
            inv_qs_list  = "dlw_pin_inv",
            validation_report_path = "validation_report",
            dlw_inv_list    = TRUE,
            dlw_qs_pinned   = TRUE,
            log             = TRUE,
            save_log        = TRUE
            )
}
}
