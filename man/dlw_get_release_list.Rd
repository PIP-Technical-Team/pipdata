% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release_mgmt.R
\name{dlw_get_release_list}
\alias{dlw_get_release_list}
\title{Retrieve a DLW release from the master list \code{.qs} file}
\usage{
dlw_get_release_list(qs_path, release_label)
}
\arguments{
\item{qs_path}{Character. Path to the \code{.qs} file storing the named list of releases
(as created by \code{dlw_store_release(..., update_inventory_list=TRUE)}).}

\item{release_label}{Character. Which release to fetch, e.g. \code{"20250202_INT"}.}
}
\value{
A data frame containing the rows for the requested release. If no rows,
returns an empty data frame.
}
\description{
This function reads a named list of releases in a \code{.qs} file (the master list),
checks that \code{release_label} exists, and reconstructs the data frame from
the stored row-based structure (\code{$data}). If the release has no rows, it returns
an empty data frame with no columns.
}
\examples{
\dontrun{
df <- dlw_get_release_list(
  qs_path       = "data/pip_raw_inventory_releases.qs",
  release_label = "20250202_INT"
)
}
}
