% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode_spec.R
\name{apply_recode_spec}
\alias{apply_recode_spec}
\title{Apply recode specification to a data.table}
\usage{
apply_recode_spec(dt, alias = "pip_inv", verbose = TRUE)
}
\arguments{
\item{dt}{data.table with DLW survey data.}

\item{alias}{Stamp alias. Default: \code{"pip_inv"}.}

\item{verbose}{Logical. Default: \code{TRUE}.}
}
\value{
\code{dt} (modified by reference) with attribute \code{recode_spec_version_id}.
}
\description{
Syncs \code{inst/extdata/recode_spec.yml} to stamp (creates a new stamp version
only when the content has changed), then applies all matching rules to \code{dt}.
}
\details{
\strong{Replace-type recodes} (\code{range_clamp}, \code{binary_map}, \code{haven_labels}): if
\code{source_column} differs from \code{var_name}, the source column is \strong{renamed} to
\code{var_name} after the recode (dropping the source). Example: \verb{urban → area},
\verb{male → gender}.

\strong{Derive-type recodes} (\code{binned_from_continuous},
\code{quantile_from_continuous}): the source column is preserved and \code{var_name}
is added as a new column. Example: \code{age} stays, \code{age_group} is added.

The stamp \code{version_id} of the active spec is attached as attribute
\code{"recode_spec_version_id"} on the returned \code{dt}.
}
