% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_pip.R
\name{save_pip_data}
\alias{save_pip_data}
\title{Save cleaned PIP data or metadata to versioned storage}
\usage{
save_pip_data(
  data,
  alias,
  verbose = getOption("pipdata.verbose", default = TRUE)
)
}
\arguments{
\item{data}{A named list of \code{data.table} objects to save. Names
are used as the \code{id} argument to \code{\link[pipload:pip_write]{pipload::pip_write()}}.}

\item{alias}{Character scalar. The storage alias passed to
\code{\link[pipload:pip_write]{pipload::pip_write()}} (e.g., \code{"pip"} for survey data,
\code{"pip_meta"} for metadata).}

\item{verbose}{Logical. Controls verbosity of downstream
\code{\link[pipload:pip_write]{pipload::pip_write()}} calls. Default:
\code{getOption("pipdata.verbose", default = TRUE)}.}
}
\value{
A named list with one entry per artifact: \code{list(pip_id, success = TRUE)}
on success or \code{NULL} on failure. Version metadata is persisted to the
stamp catalog and read back by \code{\link[=build_pip_inventory]{build_pip_inventory()}} — it is not
returned here.
}
\description{
Iterates over a named list of cleaned \code{data.table} objects and writes
each one to the PIP storage backend via \code{\link[pipload:pip_write]{pipload::pip_write()}}.
Errors during individual saves are caught, logged, and returned as
\code{NULL} so that remaining surveys can continue.
}
\seealso{
Other pd_process_data pipeline: 
\code{\link{add_attr}()},
\code{\link{build_pip_inventory}()},
\code{\link{create_attr}()},
\code{\link{data_to_dt}()},
\code{\link{filter_aux_data}()},
\code{\link{filter_aux_inv}()},
\code{\link{fix_year_var}()},
\code{\link{inv_dlw_load}()},
\code{\link{inv_to_process}()},
\code{\link{log_report}()},
\code{\link{pd_aux_attr}()},
\code{\link{pd_deflation}()},
\code{\link{survey_id_to_attr}()},
\code{\link{valid_dlw_load}()}
}
\concept{pd_process_data pipeline}
