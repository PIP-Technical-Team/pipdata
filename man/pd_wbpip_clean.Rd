% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_wbpip_clean.R
\name{pd_wbpip_clean}
\alias{pd_wbpip_clean}
\title{Clean data for wbpip compatibility (high level)}
\usage{
pd_wbpip_clean(lf)
}
\arguments{
\item{lf}{list of dataframe returned by \code{pd_dlw_clean()}}
}
\value{
list with data.tables
}
\description{
Clean data for wbpip compatibility (high level)
}
\examples{
md   <- pipload::pip_load_dlw(country = "PHL", 2012)
pfw  <- pipload::pip_load_aux("pfw")
cpfw <- get_country_pfw(md, pfw)
ls   <- pd_cpfw_merge(md, cpfw)
ls_c    <- pd_dlw_clean(ls)
ls_f    <- pd_wbpip_clean(ls_c)[[1]]
summary(ls_f$weight)


gd   <- pipload::pip_load_dlw("CHN", 2015)
cpfw <- get_country_pfw(gd, pfw)
ls   <- pd_cpfw_merge(gd, cpfw)
ls_c    <- pd_dlw_clean(ls)
ls_f    <- pd_wbpip_clean(ls_c)[[1]]
ls_f[, unique(area)]

gd   <- pipload::pip_load_dlw("ARE", 2019)
cpfw <- get_country_pfw(gd, pfw)
ls   <- pd_cpfw_merge(gd, cpfw)
ls_c <- pd_dlw_clean(ls)
ls_f <- pd_wbpip_clean(ls_c)[[1]]
ls_f[, unique(area)]
}
