% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_report.R
\name{log_report}
\alias{log_report}
\title{Generate a markdown report from a pipeline log}
\usage{
log_report(log, path = NULL, title = "Pipeline Log Report", overwrite = FALSE)
}
\arguments{
\item{log}{A \code{piplog} object (inherits from \code{data.table}).}

\item{path}{Character scalar. File path for the output \code{.md} file.
If \code{NULL} (default), the report is returned as a character vector and
not written to disk.}

\item{title}{Character scalar. Title for the report
(default: \code{"Pipeline Log Report"}).}

\item{overwrite}{Logical. Overwrite \code{path} if it already exists
(default: \code{FALSE}).}
}
\value{
Invisibly, the report as a character vector (one element per line).
If \code{path} is non-\code{NULL}, the file is written as a side-effect.
}
\description{
Parses a \code{piplog} object produced by \code{pipfun::log_filter()} and writes a
structured markdown document summarising errors, informational messages, and
affected surveys.
}
\details{
The report contains:
\itemize{
\item Run metadata (time window, total entries, success/fail counts).
\item Processing summary: total, cleaned, and failed counts
(from \code{process_summary_inf} log entry).
\item Auxiliary file changes: which measures changed and how many
surveys were affected (from \code{aux_changes_inf} log entry).
\item Summary table by error / info type.
\item Country-level breakdown of errors.
\item Inventory verification: confirmed vs missing surveys
(from \code{inv_update_inf} log entry).
\item Surveys skipped during data processing or metadata creation
(\code{skipped_svys_data} / \code{skipped_svys_metadata} entries), with reasons.
\item List of surveys that failed processing (\code{null_svys_inf} entry).
}
Sections that rely on a specific logmeta entry are silently omitted when
that entry is absent from the log.
}
\examples{
\dontrun{
log <- pipfun::log_filter(name = "pipdata_log")
# Return as character vector
report <- log_report(log)
# Write to file
log_report(log, path = "log_report.md", overwrite = TRUE)
}
}
\seealso{
Other pd_process_data pipeline:
\code{\link[=add_attr]{add_attr()}},
\code{\link[=build_pip_inventory]{build_pip_inventory()}},
\code{\link[=create_attr]{create_attr()}},
\code{\link[=data_to_dt]{data_to_dt()}},
\code{\link[=filter_aux_data]{filter_aux_data()}},
\code{\link[=filter_aux_inv]{filter_aux_inv()}},
\code{\link[=fix_year_var]{fix_year_var()}},
\code{\link[=inv_dlw_load]{inv_dlw_load()}},
\code{\link[=inv_to_process]{inv_to_process()}},
\code{\link[=pd_aux_attr]{pd_aux_attr()}},
\code{\link[=pd_deflation]{pd_deflation()}},
\code{\link[=save_pip_data]{save_pip_data()}},
\code{\link[=survey_id_to_attr]{survey_id_to_attr()}},
\code{\link[=valid_dlw_load]{valid_dlw_load()}}
}
\concept{pd_process_data pipeline}
