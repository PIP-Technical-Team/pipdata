% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow_prep.R
\name{prepare_for_arrow}
\alias{prepare_for_arrow}
\title{Prepare a survey data.table for Arrow / Parquet writing}
\usage{
prepare_for_arrow(data, metadata, pip_id)
}
\arguments{
\item{data}{A \code{data.table} of row-level survey microdata as returned by
\code{pipload::load_pip_data(..., metadata = FALSE)}. Must contain at least
\code{welfare} and \code{weight} columns.}

\item{metadata}{Named list of survey identifiers as returned by
\code{pipload::load_pip_data(..., metadata = TRUE)}. Must contain
\code{country_code}, \code{surveyid_year}, \code{welfare_type}, \code{vermast}, and \code{veralt}.}

\item{pip_id}{The canonical \code{pip_id} string for this specific survey file
(e.g. \code{"ARG_2003_EPHC-S2_INC_ALL"}), taken from the \code{pip_id} column of
the release inventory. Stored in the \code{survey_id} column of the output
data.table and used as the Parquet filename stem.}
}
\value{
A new \code{data.table} containing only schema-allowed columns, ready
for \code{\link[=write_survey_parquet]{write_survey_parquet()}}.
}
\description{
Transforms a {pipdata} clean survey \code{data.table} and its associated
metadata list into a schema-conformant \code{data.table} ready to be passed
directly to \code{\link[=write_survey_parquet]{write_survey_parquet()}} (or \code{arrow::write_parquet()}).
}
\details{
The function applies, in order:
\enumerate{
\item Metadata injection (§1.1) — adds \code{country_code}, \code{surveyid_year},
\code{welfare_type}, \code{pip_id}, \code{version} as constant columns.
\item Type casting (§2) — \code{welfare} and \code{weight} to \code{double}.
\item Breakdown dimension standardisation (§3) — \code{gender}, \code{area},
\code{education}, \code{age} are derived/normalised where source columns exist.
\item Column selection — only schema-allowed columns are retained; all
others are dropped.  Optional breakdown columns that are entirely \code{NA}
after standardisation are also dropped (they must be absent, not all-NA).
\item Pre-write validation (§4) — aborts with a descriptive error on any
schema or data-quality violation.
}

The input \code{data.table} is \strong{copied} before transformation; the original
object passed by the caller is not modified.
}
\examples{
\dontrun{
inv  <- pipload::load_pip_master_inventory()
pip  <- inv[survey_id == "ARG_2003_EPHC-S2_V01_M_V09_A_GMD_ALL", pip_id]
raw  <- pipload::load_pip_data("ARG", 2003, "EPHC-S2", metadata = FALSE)
meta <- pipload::load_pip_data("ARG", 2003, "EPHC-S2", metadata = TRUE)
dt   <- prepare_for_arrow(raw, meta, pip_id = pip)
}
}
\seealso{
\code{\link[=write_survey_parquet]{write_survey_parquet()}}, \code{\link[=generate_arrow_dataset]{generate_arrow_dataset()}}
}
\concept{arrow-prep}
