% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_pip_inventory.R
\name{update_pip_inventory}
\alias{update_pip_inventory}
\title{Update the PIP master and release inventories with newly cleaned data}
\usage{
update_pip_inventory(inv_to_clean, proc_dta)
}
\arguments{
\item{inv_to_clean}{A \code{data.table} of the DLW surveys that were
sent for processing (as returned by \code{\link[=valid_dlw_load]{valid_dlw_load()}}).}

\item{proc_dta}{A named list of processing results, one per survey.
Each element is a list with \code{pip_names}, \code{versions_data}, and
\code{versions_metadata}, or \code{NULL} for failed surveys.}
}
\value{
A \code{data.table}: the updated PIP master inventory, including two
new columns tracking release membership:
\itemize{
\item \code{first_release_version_id}: stamp version ID of the release inventory
when this survey first appeared.
\item \code{latest_release_version_id}: stamp version ID of the most recent release
inventory that confirmed this survey.
}
}
\description{
After surveys are processed by \code{\link[=process_data]{process_data()}}, this function
merges the version metadata for data and metadata files, removes
skipped surveys, appends the new entries to the existing master
inventory, and writes both the master and release inventories
to storage via \code{\link[pipload:pip_write]{pipload::pip_write()}}.
}
\details{
Release inventory vintages are tracked via stamp's built-in version history.
To load a previous release inventory snapshot:
\code{pipload::pip_read("pip_release_inventory", version = -1, alias = "pip_inv")}

\strong{Logging}: This function writes several informational entries to the \code{"pipdata_log"}:
\itemize{
\item \code{null_svys_inf}: List of surveys that failed processing (when applicable).
\item \code{release_write_err}: Release inventory write failure.
\item \code{inv_update_inf}: Inventory verification summary showing the number of surveys
expected, confirmed in master inventory, and missing. Written as an error-level
entry if any surveys are missing, info-level if all are confirmed.
\item \code{skipped_svys_data}: Surveys skipped during data processing with reasons.
\item \code{skipped_svys_metadata}: Surveys skipped during metadata creation with reasons.
}
}
\seealso{
Other pd_process_data pipeline: 
\code{\link{add_attr}()},
\code{\link{create_attr}()},
\code{\link{data_to_dt}()},
\code{\link{filter_aux_data}()},
\code{\link{filter_aux_inv}()},
\code{\link{fix_year_var}()},
\code{\link{format_vrs}()},
\code{\link{inv_dlw_load}()},
\code{\link{inv_to_process}()},
\code{\link{log_report}()},
\code{\link{pd_aux_attr}()},
\code{\link{save_pip_data}()},
\code{\link{survey_id_to_attr}()},
\code{\link{valid_dlw_load}()}
}
\concept{pd_process_data pipeline}
