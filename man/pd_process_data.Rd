% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_process_data.R
\name{pd_process_data}
\alias{pd_process_data}
\title{Process DLW inventory and create cleaned pip data}
\usage{
pd_process_data(
  inv = NULL,
  aux_measures = c("pfw", "cpi", "ppp", "pop", "gdp", "pce"),
  force = FALSE,
  verbose = getOption("pipdata.verbose", default = TRUE)
)
}
\arguments{
\item{inv}{A data.frame or tibble containing the DLW inventory. Default
\code{NULL}, in which case it is loaded internally via
\code{pipload::load_gmd_valid_inv()}.}

\item{aux_measures}{A character vector of auxiliary measures to load and merge
with the DLW data. The default is \code{c("pfw", "cpi", "ppp", "pop", "gdp", "pce")}.}

\item{force}{Logical. If \code{TRUE}, forces reprocessing of all surveys by
switching stamp versioning to \code{"timestamp"} and bypassing the master
inventory comparison. Default \code{FALSE}.}

\item{verbose}{Logical. Print progress messages. Default:
\code{getOption("pipdata.verbose", default = TRUE)}.}
}
\value{
A data.frame: updated pip inventory (\code{new_pip_inv}) with new
versions for cleaned data and metadata.
}
\description{
Iterate over the datalibweb (DLW) inventory, process each survey by
merging auxiliary data (PFW, CPI, PPP, population, GDP, PCE), cleaning
main variables, creating metadata, and saving new versions of the cleaned
data and metadata into the pip storage. The function returns an updated
pip inventory with the new versions recorded.
}
\details{
\strong{Logging}: This function writes \code{process_summary_inf} and \code{null_svys_inf} entries
to the \code{"pipdata_log"}, summarizing totals and failed surveys. Additional entries for
auxiliary file changes and inventory verification are emitted by \code{\link[=valid_dlw_load]{valid_dlw_load()}}
and \code{\link[=build_pip_inventory]{build_pip_inventory()}} respectively.

\strong{Recode spec}: the recode specification is synced to stamp once via
\code{\link[=sync_recode_spec]{sync_recode_spec()}} before the per-survey loop and the resolved spec is
threaded into each \code{\link[=process_data]{process_data()}} call, so \code{\link[=apply_recode_spec]{apply_recode_spec()}} performs
no stamp I/O per survey.

\strong{Memory management}: surveys are processed one at a time. After each survey
is saved, the large intermediates (\code{df}, \code{ls_cpfw}, \code{ls_clean}, \code{metadata})
are explicitly removed and \code{gc()} is called inside \code{\link[=process_data]{process_data()}} before
the next survey is loaded, keeping peak heap bounded on full-inventory runs.
}
\examples{
\dontrun{
release <- "20250203"
pipfun::setup_working_release(release)
pd_process_data()
}
}
