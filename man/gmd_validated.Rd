% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipdata_validate_gmd.R
\name{gmd_validated}
\alias{gmd_validated}
\title{Return Validated GMD Records}
\usage{
gmd_validated(gmd_new, inv_validated)
}
\arguments{
\item{gmd_new}{A data.table containing new GMD records. Must include
columns \code{FileName} and \code{Checksum}.}

\item{inv_validated}{A data.table of validated inventory records
with \code{survey_id} and \code{Checksum} columns.}
}
\value{
A data.table with only validated GMD records that exist in both \code{gmd_new} and \code{inv_validated}.
Returns \code{NULL} if \code{inv_validated} is NULL or empty. Result is returned invisibly.
}
\description{
This function filters the GMD dataset to return only the records that match entries in the validated inventory.
}
\examples{
\dontrun{
validated_gmd <- gmd_validated(gmd_new, inv_validated)
}
}
