% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_pip_inventory.R
\name{build_pip_inventory}
\alias{build_pip_inventory}
\title{Build the PIP master and release inventories from stamp catalogs}
\usage{
build_pip_inventory(
  inv_to_clean,
  pip_id_map,
  aux_hashes = NULL,
  verbose = getOption("pipdata.verbose", default = TRUE)
)
}
\arguments{
\item{inv_to_clean}{A \code{data.table} of DLW surveys sent for processing (as
returned by \code{\link[=valid_dlw_load]{valid_dlw_load()}}). Must have unique \code{survey_id} rows.}

\item{pip_id_map}{A \code{data.table} with exactly two columns: \code{survey_id}
(DLW survey identifier) and \code{pip_id} (PIP identifier, uppercase). Built
from successful \code{process_data()} calls in \code{\link[=pd_process_data]{pd_process_data()}}.}

\item{aux_hashes}{A named character vector of current aux \code{content_hash}
values, one per requested auxiliary measure (e.g. \code{cpi}, \code{ppp}, \code{pfw}).
Resolved once per run by \code{\link[=get_aux_hashes]{get_aux_hashes()}} and recorded on the
master-inventory rows produced for successfully processed surveys.
Default \code{NULL} (no aux hashes recorded).}

\item{verbose}{Logical. Controls verbosity of downstream
\code{\link[pipload:load_pip_master_inventory]{pipload::load_pip_master_inventory()}} and \code{\link[pipload:load_aux_data]{pipload::load_aux_data()}}
calls. Default: \code{getOption("pipdata.verbose", default = TRUE)}.}
}
\value{
A \code{data.table}: the updated PIP master inventory. Does \strong{not}
include \code{reporting_level} — enrich after load via
\code{pipload::pip_inv_enrich(inv, fields = "reporting_level")}.
}
\description{
Delta/update assembler replacing \code{update_pip_inventory()}. Reads version
facts for \strong{current-run surveys only} from stamp's persisted catalogs
(\code{"pip"} and \code{"pip_meta"} aliases), upserts them into the prior master
inventory, and saves the result. Old surveys not reprocessed this run are
retained unchanged from the prior master.
}
\details{
Compared to \code{update_pip_inventory()}, this function:
\itemize{
\item Queries catalogs then immediately filters to the current run's pip_ids,
avoiding all catalog-wide validation issues.
\item Does not require in-memory version metadata (crash-safe for current run).
\item Does not compute \code{reporting_level} — enrichment is handled by
\code{\link[pipload:pip_inv_enrich]{pipload::pip_inv_enrich()}} when needed by consumers.
\item Upserts by \code{pip_id}: reprocessed surveys replace their old row; all other
surveys are retained from the prior master. One row per \code{pip_id}, always.
}

\strong{Logging}: writes the following entries to \code{"pipdata_log"}:
\itemize{
\item \code{inv_update_inf}: verification that expected surveys appear in master
(info-level if all confirmed, error-level if any missing).
\item \code{release_write_err}: tryCatch-caught release inventory write failure.
}

\strong{Column provenance}:
\itemize{
\item \code{version_id_data}, \code{content_hash_data}, \code{size_bytes_data},
\code{created_at_data}, \code{path_data} â€” from the \code{"pip"} catalog.
\item \code{version_id_metadata}, \code{content_hash_metadata}, \code{size_bytes_metadata},
\code{created_at_metadata}, \code{path_metadata} â€” from the \code{"pip_meta"} catalog.
\item \code{pipeline_version_dlw}, \code{latest_version_id_dlw}, \code{content_hash_dlw},
\code{Checksum_dlw}, \code{path_dlw} â€” renamed from DLW inventory columns.
\item \code{welfare_type} â€” derived from the 4th \verb{_}-delimited segment of \code{pip_id}.
\item \verb{aux_<measure>_hash} (e.g. \code{aux_cpi_hash}, \code{aux_ppp_hash}, \code{aux_pfw_hash})
â€” current aux \code{content_hash} for each requested measure, from the
run-level \code{aux_hashes} map passed by \code{\link[=pd_process_data]{pd_process_data()}}. Only populated
for surveys successfully processed in the current run.
\item \code{first_release_version_id}, \code{latest_release_version_id} â€” stamp version
IDs of the release inventory (first appearance and most recent).
}
}
\seealso{
Other pd_process_data pipeline:
\code{\link[=add_attr]{add_attr()}},
\code{\link[=create_attr]{create_attr()}},
\code{\link[=data_to_dt]{data_to_dt()}},
\code{\link[=filter_aux_data]{filter_aux_data()}},
\code{\link[=filter_aux_inv]{filter_aux_inv()}},
\code{\link[=fix_year_var]{fix_year_var()}},
\code{\link[=get_aux_hashes]{get_aux_hashes()}},
\code{\link[=inv_dlw_load]{inv_dlw_load()}},
\code{\link[=inv_to_process]{inv_to_process()}},
\code{\link[=log_report]{log_report()}},
\code{\link[=pd_aux_attr]{pd_aux_attr()}},
\code{\link[=pd_deflation]{pd_deflation()}},
\code{\link[=save_pip_data]{save_pip_data()}},
\code{\link[=survey_id_to_attr]{survey_id_to_attr()}},
\code{\link[=valid_dlw_load]{valid_dlw_load()}}
}
\concept{pd_process_data pipeline}
