% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipdata_dlw_validation.R
\name{dlw_var_check}
\alias{dlw_var_check}
\alias{is_character}
\alias{is_numeric}
\alias{check_urban}
\alias{check_gender}
\alias{is_greaterthanzero}
\alias{is_greaterequale0}
\alias{is_var_avail}
\alias{is_var_startwith_avail}
\alias{is_valuebtwn0and110}
\title{Validating Specific Conditions of a Variable (Generic Documentation)}
\usage{
dlw_var_check(val, col_name)

is_character(val, col_name)

is_numeric(val, col_name)

check_urban(val, col_name)

check_gender(val, col_name)

is_greaterthanzero(val, col_name)

is_greaterequale0(val, col_name)

is_var_avail(val, col_name)

is_var_startwith_avail(val, col_name)

is_valuebtwn0and110(val, col_name)
}
\arguments{
\item{val}{variable name}

\item{col_name}{data}
}
\value{
a validation report as text
}
\description{
This interface serves as a generic check for variables in DLW datasets across various scenarios.
It includes specific functions designed to assess different conditions, such as determining if a variable is of character or numeric type,
checking the number of reporting levels for urban/rural variables, verifying if values are greater than zero,
and confirming the availability of a variable within the dataset.
}
\section{Functions}{
\itemize{
\item \code{is_character()}: Check a variable is character

\item \code{is_numeric()}: Check a variable is numeric

\item \code{check_urban()}: Check residential variable (urban/rural) has more than one reporting level in group data

\item \code{check_gender()}: Check gender (male - variable) has more than two categories in ALL data

\item \code{is_greaterthanzero()}: Check a numeric variable is greater than 0

\item \code{is_greaterequale0()}: Check a numeric variable is greater than or equal to 0

\item \code{is_var_avail()}: Check a variable is available in a dataset with specified variable name

\item \code{is_var_startwith_avail()}: Check a variable is available in a dataset with variable name starting with a specified text

\item \code{is_valuebtwn0and110()}: Check age is available in a dataset with value between 0 and 110

}}
\examples{
\dontrun{
is_character(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_numeric(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
check_urban(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
check_gender(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_greaterthanzero(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_greaterequale0(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_var_avail(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_var_startwith_avail(
  val = data,
  col_name = variable_name,
)
}
\dontrun{
is_valuebtwn0and110(
  val = data,
  col_name = variable_name,
)
}
}
\keyword{internal}
