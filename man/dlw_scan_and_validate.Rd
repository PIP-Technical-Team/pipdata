% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dlw_scan_and_validate.R
\name{dlw_scan_and_validate}
\alias{dlw_scan_and_validate}
\title{Scan DLW .qs files, validate them, and version them in pip_raw_inventory}
\usage{
dlw_scan_and_validate(
  dlw_qs_folder,
  pip_raw_folder,
  pip_raw_inventory_path,
  validation_report_path,
  log = TRUE
)
}
\arguments{
\item{dlw_qs_folder}{Character. Path to folder containing .qs files converted from DLW-RAW.}

\item{pip_raw_folder}{Character. Folder into which validated .qs files
(versioned) will be placed.}

\item{pip_raw_inventory_path}{Character. Path where the pip_raw_inventory.qs
file is stored (or will be created if missing).}

\item{validation_report_path}{Character. Path to validation report}

\item{log}{TRUE/FALSE default value is \code{TRUE}}
}
\value{
Invisibly returns the updated pip_raw_inventory as a data.frame.
}
\description{
This function checks a folder of .qs files (dlw_qs_folder), compares them to
an existing pip_raw_inventory (pip_raw_inventory_path), and performs
validation. It then versions and copies valid files into a pip_raw_folder,
updating the inventory with the new file information.
}
\examples{
\dontrun{
dlw_scan_and_validate(
  dlw_qs_folder = "data/dlw_qs",
  pip_raw_folder = "data/pip_raw",
  pip_raw_inventory_path = "data/pip_raw_inventory.qs"
)
}
}
