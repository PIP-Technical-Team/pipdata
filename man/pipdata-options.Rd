% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipdata-options.R
\name{pipdata-options}
\alias{pipdata-options}
\title{pipdata package options}
\description{
\code{pipdata} is controlled by a small set of \code{\link[=options]{options()}}. Each option has a
package-level default set in \code{.onLoad()} (see \code{zzz.R}) and can be
overridden per-session or per-call.
}
\section{Options}{


\strong{\code{pipdata.verbose}} (\code{logical}, default \code{TRUE})

Controls whether informational I/O messages from downstream packages
(\code{pipload}, \code{stamp}, \code{pipaux}) are printed during pipeline runs.
\itemize{
\item \code{TRUE} (default): \code{pip_read()}, \code{pip_write()},
\code{load_pip_master_inventory()}, \code{load_aux_data()}, and related I/O
calls emit their normal progress messages. Useful for interactive
exploration.
\item \code{FALSE}: all downstream I/O messages are suppressed. Recommended for
batch/production runs to keep output focused on pipeline-level events.
}

The option is read at the boundary of every exported function via
\code{getOption("pipdata.verbose", default = TRUE)}. You can still override it
per-call by passing \code{verbose = FALSE} (or \code{TRUE}) explicitly — the
per-call argument always takes precedence over the global option.

All \code{pipload}/\code{stamp}/\code{pipaux} I/O calls propagate the resolved value
without exception. The only calls hardcoded to \code{verbose = FALSE} are
\verb{joyn::} join diagnostics, which are not I/O messages and outside the
scope of this option.

\strong{Batch run pattern} — set at the top of your orchestration script:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(pipdata.verbose = FALSE)
}\if{html}{\out{</div>}}

\strong{Note}: \code{pipdata.verbose} controls \emph{user-facing I/O messages} only.  It
does \strong{not} affect the structured pipeline logging written via
\code{pipfun::log_add()} / \code{pipfun::log_info()} into \code{pipdata_log}. Structured
log entries are always emitted regardless of this option.

\strong{\code{pipdata.dependency_manifest_path}} defaults to the package user cache.
Production must override it with a durable shared path scoped to the release
repository. \strong{\code{pipdata.dependency_scope}} is an optional additional
namespace; it never replaces release, identity, and alias-root isolation.
\strong{\code{pipdata.manifest_checkpoint_n}} (25) and
\strong{\code{pipdata.manifest_checkpoint_seconds}} (60) bound verified checkpoint
batches. Stamp remains the artifact version store; this manifest stores
rebuildable dependency provenance.
}

\keyword{internal}
