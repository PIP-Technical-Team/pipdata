% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow_prep.R
\name{inject_metadata_cols}
\alias{inject_metadata_cols}
\title{Inject survey metadata as constant columns into a microdata data.table}
\usage{
inject_metadata_cols(dt, metadata, pip_id)
}
\arguments{
\item{dt}{A \code{data.table} of survey microdata. Modified \strong{by reference}.}

\item{metadata}{Named list returned by
\code{pipload::load_pip_data(..., metadata = TRUE)}.  Must contain
\code{country_code}, \code{surveyid_year}, \code{welfare_type}, \code{vermast}, and \code{veralt}.}

\item{pip_id}{The canonical \code{pip_id} string for this specific survey file
(e.g. \code{"ARG_2003_EPHC-S2_INC_ALL"}), taken from the release inventory.
This is stored in the \code{survey_id} column of the output data.table and
used as the Parquet filename stem.}
}
\value{
\code{dt} invisibly (modified by reference).
}
\description{
Adds \code{country_code}, \code{surveyid_year}, \code{welfare_type}, \code{pip_id},
and \code{version} columns by reference. \code{welfare_type} is recoded from the
full string (\code{"income"} / \code{"consumption"}) to the two-letter code
(\code{"INC"} / \code{"CON"}). Unknown values are passed through after \code{toupper()}.
}
\details{
The \code{survey_id} column in the output holds the \strong{\code{pip_id}} value (e.g.
\code{"ARG_2003_EPHC-S2_INC_ALL"}), not the raw \code{survey_id} string. This is
because \code{pip_id} uniquely identifies a single physical file (one welfare
type), whereas \code{survey_id} can map to multiple files (INC + CON). The
Parquet filename and partition path are derived from this column.
}
\keyword{internal}
