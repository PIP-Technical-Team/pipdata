# Validate validation spec schema

Validate validation spec schema

## Usage

``` r
validate_validation_spec(spec)
```

## Arguments

- spec:

  A parsed validation spec list.

## Value

`TRUE` if valid.
