# Add metadata as attributes to data.table

Add metadata as attributes to data.table

## Usage

``` r
add_main_att(dt, cpfw)
```

## Arguments

- dt:

  data.table

- cpfw:

  data.table with country/survey PFW

## Value

data.table
