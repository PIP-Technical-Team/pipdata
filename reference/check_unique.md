# Check unique values in aux changes

According to the aux key, this function selects the unique values.

## Usage

``` r
check_unique(x, key = attributes(x)$key_cols)
```

## Arguments

- x:

  data.frame with aux changes for specific aux file

- key:

  variables that define difference

## Value

data.frame
