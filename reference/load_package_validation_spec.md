# .validation_spec_cache is stored in .pipdataenv to avoid locked-binding issues Load the package validation spec

.validation_spec_cache is stored in .pipdataenv to avoid locked-binding
issues Load the package validation spec

## Usage

``` r
load_package_validation_spec()
```

## Value

A parsed validation spec list.
