# Get a simple frequency that shows number of valid and invalid datasets

Get a simple frequency that shows number of valid and invalid datasets

## Usage

``` r
get_data_status()
```

## Value

a table

## Examples

``` r
if (FALSE) { # \dontrun{
get_data_status()
} # }
```
